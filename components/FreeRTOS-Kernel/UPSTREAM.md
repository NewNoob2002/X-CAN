# FreeRTOS Kernel upstream

- Project: FreeRTOS-Kernel
- Release: V11.3.0
- License: MIT (`LICENSE.md`)

This snapshot contains the kernel sources used by X-CAN and the unchanged GCC
ARM_CM4F port. `Core/Inc/FreeRTOSConfig.h` is the project configuration and is
kept outside the upstream snapshot.
